import{motion,AnimatePresence}from'framer-motion';import{Menu,X}from'lucide-react';type Props={open:boolean;setOpen:(v:boolean)=>void};const links=['home','about','projects','skills','experience','resume','contact'];export function PokeballNav({open,setOpen}:Props){const go=(id:string)=>{document.getElementById(id)?.scrollIntoView({behavior:'smooth'});setOpen(false)};return <div className="ball-nav"><motion.button aria-label="Open world navigation" aria-expanded={open} className="pokeball" onClick={()=>setOpen(!open)} whileHover={{scale:1.08}} whileTap={{scale:.9}}>{open?<X/>:<Menu/>}</motion.button><AnimatePresence>{open&&<motion.div className="ball-menu" initial={{opacity:0,scale:.7,y:20}} animate={{opacity:1,scale:1,y:0}} exit={{opacity:0,scale:.7,y:20}}>{links.map(x=><button key={x} onClick={()=>go(x)}>{x}</button>)}</motion.div>}</AnimatePresence></div>}
export function BallIcon(){return <span className="mini-ball" aria-hidden="true"><i/></span>}
